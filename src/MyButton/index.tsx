import React from 'react';
import { Button } from 'antd'

export default ({ title }: { title: string }) => <Button type="primary" onClick={() => console.log('Button')}>我是antd的按钮43214123123324</Button>;
